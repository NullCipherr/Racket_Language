#lang racket

; Binary Tree
;
; Uma Arvore binária pode ser empty ou (arvore-bin valor_armazenado esquerda direita).
;

(struct arvore-binaria (x esq dir))

(define arvore-1 (arvore-binaria 2 empty empty))